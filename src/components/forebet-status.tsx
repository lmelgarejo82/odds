import { database } from "@/infrastructure/database";
const formatter=new Intl.DateTimeFormat("es-PY",{timeZone:"America/Asuncion",dateStyle:"medium",timeStyle:"medium"});
export async function ForebetStatus(){const latest=await database.forebetCaptureAttempt.findFirst({orderBy:{capturedAt:"desc"},include:{snapshot:true}});if(!latest)return <section className="panel forebet-status"><span className="eyebrow">Fuente · Forebet</span><h2>Sin capturas</h2><p>Ejecute la captura controlada para 2026-07-21 mediante el comando documentado.</p></section>;const snapshot=latest.snapshot;return <section className="panel forebet-status"><span className="eyebrow">Fuente · Forebet</span><div className="section-heading"><h2>{latest.status}</h2><span className="date">{formatter.format(latest.capturedAt)}</span></div><dl><div><dt>Fecha deportiva</dt><dd>{latest.requestedDate.toISOString().slice(0,10)}</dd></div><div><dt>Filas encontradas</dt><dd>{snapshot?.rowsFound??0}</dd></div><div><dt>Observaciones válidas</dt><dd>{snapshot?.validRows??0}</dd></div><div><dt>Rechazadas</dt><dd>{snapshot?.rejectedRows??0}</dd></div><div><dt>Warnings</dt><dd>{snapshot?.warningCount??0}</dd></div><div><dt>SHA-256</dt><dd>{latest.contentHash?.slice(0,12)??"—"}</dd></div><div><dt>Parser</dt><dd>{latest.parserVersion}</dd></div><div><dt>Calidad</dt><dd>{latest.status==="FAILED"?"Fallo cerrado":snapshot?.rejectedRows?"Parcial":"Controlada"}</dd></div></dl></section>}
